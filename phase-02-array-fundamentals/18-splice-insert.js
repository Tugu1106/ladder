// Exercise 18 — splice insert
// Instructions: 18-splice-insert.md
//
// Goal: insert "yellow" between "red" and "blue". Nothing gets deleted.
//
// Note: splice(startIndex, 0, "newValue") — the 0 means "delete nothing",
//       which turns a removal into a pure insertion.
//
// Expected output:
//   [ 'red', 'yellow', 'blue' ]

const colors = ["red", "blue"];

// --- your code below ---
