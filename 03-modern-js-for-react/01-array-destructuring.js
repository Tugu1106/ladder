// ✅ DONE — correct.
// Exercise 1 — Array Destructuring
// Phase 2: Modern JS for React
//
// Goal: pull the first two colors into their own variables using ARRAY
//       destructuring, then print them.
//
//   const [first, second] = colors;
//
// Arrays destructure by POSITION — first variable = index 0, second = index 1.
//
// Expected output:
//   red
//   blue

const colors = ["red", "blue", "green"];

// --- your code below ---
const [first, second] = colors;

console.log(first);
console.log(second);
