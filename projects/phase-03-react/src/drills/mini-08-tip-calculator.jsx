// Mini 08 — Tip Calculator
// Goal: enter a bill amount and a tip %, show the tip and the total.
// NEW idea: DERIVED values from MULTIPLE inputs — compute during render.
//   const tip = bill * (percent / 100);
//   const total = bill + tip;
//   (Don't store tip/total in state — just calculate them each render.)
// Build: bill state + percent state (number inputs); show tip and total.
// Tip: convert the input strings with Number(...).

import { useState } from "react";

function Drill() {
  // bill + percent states; compute tip and total; display them
}

export default Drill;
