// Mini 13 — Contact Form
// Goal: name + email inputs; on Submit, add the entry to a list below.
// NEW idea: MULTIPLE controlled inputs + a real FORM submit.
//   <form onSubmit={(e) => {
//     e.preventDefault();            // stop the page reloading (Phase-1 lesson)
//     // add { name, email } to entries, then clear both inputs
//   }}>
// Build: name state + email state; entries array state; submit adds an entry and
//        clears the inputs; map entries below.

import { useState } from "react";

function Drill() {
  // name + email states; entries array; a <form onSubmit>; list the entries
}

export default Drill;
