// Exercise 2 — Double Even Numbers
// Instructions: 02-double-even-numbers.md
//
// Goal: keep even numbers -> double them -> [4,8,12]
//
// A case where the WRONG order still produces an answer, just a wrong one:
// double first and everything becomes even, so you'd get six items instead of
// three. No error, no crash — it just lies to you.
//
// Run it the wrong way once and look at the six-item result. When a pipeline
// gives a plausible but wrong answer, order is the first thing to check.
//
// Expected output:
//   [ 4, 8, 12 ]

const numbers = [1, 2, 3, 4, 5, 6];

// --- your code below ---
