// Mini 09 — Temperature Converter
// Goal: type Celsius, show Fahrenheit live.
// NEW idea: another derived value — F = C * 1.8 + 32 (from your DS course!).
// Build: celsius state (number input); compute fahrenheit; show both.
// Bonus: convert the other way too.

import { useState } from "react";

function Drill() {
  // celsius state; compute fahrenheit; display both
}

export default Drill;
